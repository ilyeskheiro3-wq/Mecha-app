import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maintenance.scheduler',
  appName: 'Maintenance Scheduler',
  webDir: 'out'
};

export default config;
